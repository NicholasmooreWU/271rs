[package]
name = "ed25519_manual"
version = "0.1.0"
edition = "2021"

[lib]
name = "ed25519_manual"
path = "src/lib.rs"

[dependencies]
num-bigint = "0.4"
num-traits = "0.2"
sha2 = "0.10"
hex = "0.4"
zeroize = { version = "1.5", optional = true }

[dev-dependencies]
# keep tests simple for now

